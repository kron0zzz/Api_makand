# Api_makand


se crea el .env  (por ahora no está funcionando, está manual dentro de database.js)
se instala pg y env: npm install pg dotenv

### esto ya no... tal vez a futuro
se instala prisma: npm install prisma @prisma/client
se inincia prisma: npx prisma init